import { CallButtonDirective } from './call-button.directive';

describe('CallButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CallButtonDirective();
    expect(directive).toBeTruthy();
  });
});
